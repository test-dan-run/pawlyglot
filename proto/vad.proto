syntax = "proto3";

package vad;

import "base.proto";

service VoiceActivityDetector {
    rpc detect(stream DetectRequest) returns (DetectResponse) {}
}

message DetectRequest {
    bytes buffer = 1;
}

message DetectResponse {
    repeated base.Timestamp timestamps = 1;
}
